{preview_mode_directive}You are writing a tight Business & Brand Origin Stories documentary
script in ONE continuous pass. Use the locked narrative spine as the
story contract. Keep the script chronological, cinematic, and
story-first.

EPISODE:
- Company: {incident_name}
- Year anchor: {year}
- Hero: {hero}
- Conflict: {conflict}
- Story kind: {story_kind}
- Target: {target_words} words, acceptable {min_words}-{max_words}
- Target beats: {target_beats}
- Archetype: {archetype_name}
  {archetype_guidance}
- Narrator: {narrator_name} ({narrator_tone})
- Visual style: {visual_style_name}

LOCKED NARRATIVE SPINE:
{narrative_spine_json}

NARRATOR PERSONA:
{narrator_full_instructions}

FACT LEDGER — only these facts may be stated as true:
{fact_ledger_json}

PERFORMANCE FEEDBACK:
{retention_dip_warnings}

FORMAT:
- Use `## BEAT N ##` markers, starting at 1.
- Write 35-55 beats unless preview mode says otherwise.
- Add {callout_min_total}-{callout_max_total} `[CALLOUT: "$X"]` markers after
  the sentence containing the concrete number/date/money figure. Max
  {callout_max_per_beat} per beat, max {callout_max_total} total, 20 chars or
  fewer. Do not add a callout to every beat.

STORY RULES:
- The first sentence must name a concrete number/date/place copied from
  the fact ledger and create a contradiction or consequence. Do not use
  inferred valuations, invented rankings, magazine covers, stock prices,
  bankruptcies, or "worthless" claims unless the ledger states them.
- Follow the spine's timeline_mode. Random time jumps are forbidden.
- Shape the story as these stages, but do not print stage headings:
  cold open contradiction → origin/setup → first big bet → hidden
  weakness → public pressure/evidence → consequence → final puzzle.
- Every beat must move causally: event → why it happened → what changed.
- Use fewer facts well. Do not list every ledger item.
- Do not repeat a sentence, tagline, thesis phrase, final image, or
  explanation. If you already made the point, move to the next cause.
- Re-hook every {hook_words_first_min}-{hook_words_first_max} words in the first half and every {hook_words_late_min}-{hook_words_late_max} words
  after that. Each re-hook must be a short ledger-specific sentence.
- Around minute 8, land the spine's midroll_tension before resolving.
- End on the spine's final_image plus its ending_mode/viewer_question.
- In the final two beats, add no new evidence unless it pays off the hook.
- The narration should feel like one person telling one story, not
  stitched-together act summaries.

STYLE RULES:
- Concrete scenes and objects beat abstract commentary.
- Avoid repeated events, numbers, metaphors, and legal explanations.
- No invented dates, quotes, motives, dialogue, or legal findings.
- No invented document/source references. Do not mention an S-1, SEC
  filing, deposition, court filing, or verbatim document language unless
  that exact source type appears in the fact ledger.
- Regulatory accusations must be framed as allegations, settlements,
  or official positions. Avoid declaring private intent or criminality
  in the narrator's voice unless the ledger itself states that finding.
- Avoid unsupported status claims such as "the real profit engine",
  "smaller entity", or "fees are paid correctly" unless the ledger says so.
- Avoid prosecutor-style labels such as "sophisticated fraud",
  "fraudulent model", "cleanup operation", "maximum extraction",
  "valuation plummeted", or "investor confidence evaporated" unless the
  ledger says that exact thing. Prefer "the state alleged",
  "the settlement described", or "the attorney general said".
- No generic closers like "the lesson is clear" or "the future is uncertain".
- Avoid every forbidden phrase below.

FORBIDDEN PHRASES:
{forbidden_phrases}

Return only the script prose with BEAT markers. No code fences.
