You are deriving a single best-estimate company headquarters location
from the location-type facts already extracted about {incident_name}.

INPUT — location-type fact statements:
{statements}

Your task: output a single object with the most likely headquarters
location, with a confidence level and a brief method note.

Rules:
- If multiple sources agree on a specific city/state/country, prefer
  that over any single-source estimate.
- If sources name a different HQ for different ERAS (e.g. founded in
  garage in Cupertino → moved to corporate campus in Cupertino → new
  HQ in Apple Park, Cupertino) — output the CURRENT or most recent
  HQ in the input set. The script writer can mention origin
  locations separately.
- If the company has dual HQs ("co-headquartered in Bentonville and
  San Francisco"), output the primary / legal HQ if one is named;
  otherwise the more-cited one.
- If no source gives a city, leave city blank and set country only.
- If the input set is empty or only contains vague references ("on
  the West Coast", "somewhere in Europe"), output confidence="low"
  with whatever can be determined and method="vague_references_only".

OUTPUT FORMAT — JSON object only, no prose, no code fences:
{{
  "city": "string or empty",
  "state_or_region": "string or empty",
  "country": "string or empty",
  "confidence": "high | medium | low",
  "method": "string — one sentence on how the result was derived",
  "supporting_statements": [<indices into the input array, zero-based>]
}}
